import React from "react";
import {ActivityIndicator} from "react-native-paper";

// v2: some fancy loading view
export default function LearningStartingLoadingView() {
    return (
        <ActivityIndicator/>
    )
}